# Private family data — NEVER commit
jottenbourg.ged
*.ged.bak
*_augmented.ged

# Build artifacts
tree.json
tree.json.new

# Auth + v2 state (PII — never commit)
.password
users.json
augment.json
invites.json

# Brainstorming companion artifacts
.superpowers/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
__pycache__/
.pytest_cache/
